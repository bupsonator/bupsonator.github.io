Classes:
index (used for body)
grid (grid container)
oddgrid (odd grid container)

IDs:
menu (the main menu)    
    main (the main grid within the main menu)
    window (the window game)
        frm (window frame)
        fs (fullscreen button)
    fullscreen(fullscreen mode)
        minimize (minimize button)
        fullframe (fullscreen iframe)